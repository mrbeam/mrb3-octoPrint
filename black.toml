[tool.black]
line-length = 120
include = '''

(
    (
        src
      | tests
      | .github
    ).*\.pyi?$
)|(
    setup\.py
)
'''
exclude = '''

(
    src/octoprint/vendor  # we want to still be able to apply patches from upstream w/o
                          # match issues
)
'''
